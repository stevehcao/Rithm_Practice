/**
 * function accepts two params array of positive integers and a positive integer
 * returns the minimal length of a contiguous subarray which the sum is greater than or equal
 * to the target integer if there isn't one return 0 instead
 */

function minSubArrayLen(arr, target) {
  // minimum length in which an array has a sum euqal to or larger
  // than target
}
